version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
      - LEONARDO_API_KEY=${LEONARDO_API_KEY}
    restart: always

# Simplified for the current architecture
